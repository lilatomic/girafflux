---g
from noaa
|@ start -30d
| group()
| keep(columns: ["location"])
| unique(columns: "location")
---f
from(bucket: "noaa")
|> range(start: -30d)
|> group()
|> keep(columns: ["location"])
|> unique(columns: "location")
